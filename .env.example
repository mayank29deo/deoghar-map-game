# Optional — without these the game uses a local (this-device) leaderboard.
# Create a free Supabase project, run supabase/schema.sql in its SQL editor,
# then copy these from Project Settings → API.
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
